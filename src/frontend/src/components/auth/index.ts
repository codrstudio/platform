/**
 * Auth Components - Global Platform Infrastructure
 *
 * These components are part of the global platform, NOT from the auth module.
 * The auth module is purely declarative (configuration only).
 *
 * SPEC-AUTH-INF-001: Authentication infrastructure provided by global AuthContext
 * SPEC-AUTH-R-002: Auth module does NOT provide UI components
 */

export { UserAvatar } from './UserAvatar';
export { LogoutMenuItem } from './LogoutMenuItem';
